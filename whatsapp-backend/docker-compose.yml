version: '3.8'

services:
  whatsapp-backend:
    build: .
    container_name: whatsapp-backend
    ports:
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/app
